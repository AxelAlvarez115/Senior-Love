<div class="admin-page-header">
    <h1 class="admin-page-header__title">Affinités</h1>
    <div style="display:flex;gap:0.75rem;align-items:center;">
        <span class="admin-page-header__count"><%= affinities.length %> résultat<%= affinities.length > 1 ? 's' : '' %></span>
        <button class="admin-create-btn" onclick="openAdminModal('modal-affinity-create')">
            <i class='bx bx-plus'></i> Ajouter
        </button>
    </div>
</div>

<div class="admin-table-wrapper">
    <div class="admin-table-header">
        <span class="admin-table-header__title">Liste des affinités</span>
        <span class="admin-table-header__count"><%= affinities.length %></span>
    </div>
    <table class="table">
        <thead>
            <tr>
                <th class="table__header-cell">#</th>
                <th class="table__header-cell">Nom</th>
                <th class="table__header-cell">Modifier</th>
                <th class="table__header-cell">Actions</th>
            </tr>
        </thead>
        <tbody>
            <% for (const affinity of affinities) { %>
                <tr class="table__row">
                    <td class="table__cell table__cell--id"><%= affinity.id %></td>
                    <td class="table__cell"><%= affinity.name %></td>
                    <td class="table__cell">
                        <form action="/affinities/update/<%= affinity.id %>" method="POST" class="table__inline-form">
                            <input type="text" name="name" value="<%= affinity.name %>" class="table__input" required maxlength="20">
                            <button type="submit" class="table__action-btn table__action-btn--save" title="Sauvegarder">
                                <i class='bx bx-check'></i>
                            </button>
                        </form>
                    </td>
                    <td class="table__action-cell">
                        <div class="table__actions">
                            <form action="/affinities/delete/<%= affinity.id %>" method="POST">
                                <button type="submit" class="table__action-btn table__action-btn--delete" title="Supprimer">
                                    <i class='bx bx-trash'></i>
                                </button>
                            </form>
                        </div>
                    </td>
                </tr>
            <% } %>
        </tbody>
    </table>
</div>

<!-- Modal création affinité -->
<div id="modal-affinity-create" class="modal hidden">
    <div class="modal__content">
        <div class="modal__header">
            <h2 class="modal__title">Ajouter une affinité</h2>
            <button class="modal__close" onclick="closeAdminModal('modal-affinity-create')">&times;</button>
        </div>
        <form action="/admin/affinities/create" method="POST">
            <div class="modal__body modal__body--single">
                <div class="modal__field modal__field--full">
                    <label class="modal__label" for="a-name">Nom *</label>
                    <input class="modal__input" type="text" id="a-name" name="name" required maxlength="20" placeholder="Ex : Musique classique">
                </div>
            </div>
            <div class="modal__footer">
                <button type="button" class="modal__btn modal__btn--cancel" onclick="closeAdminModal('modal-affinity-create')">Annuler</button>
                <button type="submit" class="modal__btn modal__btn--primary">Créer</button>
            </div>
        </form>
    </div>
</div>

<script src="/js/adminCreate.js" defer></script>
